<UserControl x:Class="BookstoreManagement.Components.ProductCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BookstoreManagement.Components"
             mc:Ignorable="d" 
             d:DesignHeight="260" d:DesignWidth="210">
    <UniformGrid Rows="2">
        <Border x:Name="Image" Height="138"/>
        <Grid Margin="12 15 12 12" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="40"/>
            </Grid.ColumnDefinitions>

            <UniformGrid Rows="3">
                <TextBlock/>
                <TextBlock Margin="0 6 0 0"/>
                <UniformGrid Rows="2" Margin="0 6 0 0">
                    <TextBlock/>
                    <TextBlock/>
                </UniformGrid>
            </UniformGrid>
            <Border 
                Width="40"
                Height="28"
                VerticalAlignment="Top" HorizontalAlignment="Right" Grid.Column="2"/>
            <Border Width="24" Height="24"
                VerticalAlignment="Bottom" HorizontalAlignment="Right" Grid.Column="2"/>
        </Grid>
    </UniformGrid>
</UserControl>
