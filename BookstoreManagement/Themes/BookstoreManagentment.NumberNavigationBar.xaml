<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:BookstoreManagement.Components">
    <Style TargetType="{x:Type local:NumberNavigationBar}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:NumberNavigationBar}">
                    <Grid>
                        <StackPanel
                            x:Name="PART_Container"
                            local:PanelSpacingAssists.Margin="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:NumberNavigationBar}}, Path=Spacing}">

                        </StackPanel>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>